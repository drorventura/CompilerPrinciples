/* reminder.asm */

REMAINDER:
	PUSH(FP);
	MOV(FP,SP);
	PUSH(R1);
	PUSH(R2)
	MOV(R1,INDD(FPARG(2),1));
	MOV(R2,INDD(FPARG(3),1));
	REM(R1,R2);
	PUSH(R1);
	CALL(MAKE_SOB_INTEGER);

	DROP(1);
	POP(R2);
	POP(R1);
	POP(FP);
	RETURN;

